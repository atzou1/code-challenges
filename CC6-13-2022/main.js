// Create a function that takes a list of positive integers and strings and returns a new list with the strings filtered out.

// Example
// filter_list([1,2,'a','b']) == [1,2]
// filter_list([1,'a','b',0,15]) == [1,0,15]
// filter_list([1,2,'aasf','1','123',123]) == [1,2,123]

//======= WORK ======//
function filter_list(l) {
    //P: parameter with positive integers and strings
    //R: return new list with strings filtered out
    //E: 
      //filter_list([1,2,'a','b']) == [1,2]
    //P:
      //
    let result = [ , ]
    for(let i = 0; i !== ''; i++){
        return l.split(' ').replace('', ' ').join('')
    }
  }